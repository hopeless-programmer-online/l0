{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "node",
            "request": "launch",
            "console": "integratedTerminal",
            "runtimeExecutable": "npx",
            "args": [
                "ts-node",
                "${workspaceFolder}/src/cli.ts",
                "${file}"
            ]
        }
    ]
}
